// import { render, screen } from '@testing-library/react';

// import Button from './Button';

describe('Button', () => {
  it('applies the default type of button', () => {});

  it('applies the specific type if provided', () => {});

  it('applied a valid attribute to the element', () => {});
});
